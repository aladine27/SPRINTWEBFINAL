{% extends 'base.html.twig' %}

{% block title %}Edit Votes{% endblock %}
{% block header %}{% endblock %}
{% block body %}
  <div class="container">
    <div class="card">
      <div class="card-header">
        <h1 class="card-title">Edit Votes</h1>
        <a href="{{ path('app_votes_index') }}" class="btn btn-primary"><i class="bi bi-arrow-left"></i> Back to List</a>
      </div>
      <div class="card-body">
      
        {{ include('votes/_form.html.twig', {'button_label': 'Update'}) }}

        <hr>

      
      </div>
    </div>
  </div>
{% endblock %}
